<Field Horizontal="true">
    <FieldLabel ColumnSize="ColumnSize.Is4">Indbetaler</FieldLabel>
    <FieldBody ColumnSize="ColumnSize.Is8">
        <Validation Validator="@Validate">
            <Select Class="form-control child" TValue="string" @ref="@select" SelectedValue="@selectedPaymentId" SelectedValueChanged="@SetPaymentIdAsync">
                <ChildContent>
                    <SelectItem Value="@noPaymentId">Vælg indbetaler</SelectItem>
                    @if (Debtors is not null)
                    {
                        foreach (var debtor in Debtors)
                        {
                            <SelectItem Value="@debtor.PaymentId">@debtor.PaymentId (@debtor.UserInformation.FullName)</SelectItem>
                        }
                    }
                </ChildContent>
                <Feedback>
                    <ValidationError/>
                </Feedback>
            </Select>
        </Validation>
    </FieldBody>
</Field>
