@inject FormattingService FormattingService
@if (Amount != 0)
{
    var amount = Math.Abs(Amount);
    var debitCredit = Amount > 0 ? "D" : "K";
    <text>@FormattingService.FormatMoneyShort(amount) @debitCredit</text>
}
@code
{
    [Parameter]
    public int Amount { get; set; }
}